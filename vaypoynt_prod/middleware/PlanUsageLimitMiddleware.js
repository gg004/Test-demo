const config = require("./../config");
const StripeService = new (require("./../services/StripeService"))();

module.exports = async function (req, res, next) {
  /**
   * check coming charge of a specific sub
   */
  next();
};
